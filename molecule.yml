---
driver:
  name: docker
docker:
  containers:
    - name: ansible-role-lldpad
      image: giovtorres/docker-centos7-systemd
      image_version: latest
      privileged: True
verifier:
  name: testinfra
